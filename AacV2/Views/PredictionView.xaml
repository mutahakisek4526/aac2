<UserControl x:Class="AacV2.Views.PredictionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="360" Margin="6" FontSize="20" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="更新" Command="{Binding RefreshCommand}" />
        </StackPanel>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Predictions}" Margin="6">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" Margin="4" Padding="8">
                        <TextBlock Text="{Binding}" FontSize="24" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
