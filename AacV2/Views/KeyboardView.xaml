<UserControl x:Class="AacV2.Views.KeyboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding CurrentText, UpdateSourceTrigger=PropertyChanged}" FontSize="36" MinHeight="64" Margin="6" />

        <UniformGrid Grid.Row="1" Columns="10">
            <ItemsControl ItemsSource="{Binding KanaGroups}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="10" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" Command="{Binding DataContext.SelectGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </UniformGrid>

        <UniformGrid Grid.Row="2" Columns="5">
            <ItemsControl ItemsSource="{Binding CurrentCharacters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" FontSize="34" Command="{Binding DataContext.AddCharacterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </UniformGrid>

        <StackPanel Grid.Row="3">
            <WrapPanel>
                <Button Content="一文字削除" Command="{Binding DeleteCharCommand}" />
                <Button Content="全消去" Command="{Binding ClearCommand}" />
                <Button Content="確定" Command="{Binding ConfirmCommand}" />
                <Button Content="読み上げ" Command="{Binding SpeakCommand}" />
                <Button Content="停止" Command="{Binding StopSpeakCommand}" />
            </WrapPanel>
            <TextBlock Text="予測候補（最大10件）" FontSize="20" Margin="8,12,8,4" />
            <WrapPanel>
                <ItemsControl ItemsSource="{Binding Predictions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" Command="{Binding DataContext.ApplyPredictionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
