<UserControl x:Class="AacV2.Views.PhrasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="360" Margin="6" FontSize="20" Text="{Binding NewText, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="180" Margin="6" FontSize="20" Text="{Binding NewCategory, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="追加" Command="{Binding AddPhraseCommand}" />
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Phrases}" Margin="6">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="True">
                        <Button Content="削除" DockPanel.Dock="Right" Command="{Binding DataContext.DeletePhraseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                        <TextBlock Text="{Binding Name}" FontSize="24" Margin="8" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
