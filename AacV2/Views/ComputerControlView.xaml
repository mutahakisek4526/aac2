<UserControl x:Class="AacV2.Views.ComputerControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <WrapPanel>
            <Button Content="Enter" Command="{Binding EnterCommand}" />
            <Button Content="Space" Command="{Binding SpaceCommand}" />
            <Button Content="Ctrl+C" Command="{Binding CtrlCCommand}" />
        </WrapPanel>
        <WrapPanel Grid.Row="1">
            <Button Content="左クリック" Command="{Binding LeftClickCommand}" />
            <Button Content="右クリック" Command="{Binding RightClickCommand}" />
            <Button Content="ダブルクリック" Command="{Binding DoubleClickCommand}" />
        </WrapPanel>
        <WrapPanel Grid.Row="2">
            <Button Content="↑" Command="{Binding MoveUpCommand}" />
            <Button Content="↓" Command="{Binding MoveDownCommand}" />
            <Button Content="←" Command="{Binding MoveLeftCommand}" />
            <Button Content="→" Command="{Binding MoveRightCommand}" />
        </WrapPanel>
    </Grid>
</UserControl>
