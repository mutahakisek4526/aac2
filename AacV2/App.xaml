<Application x:Class="AacV2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:AacV2.Behaviors"
             xmlns:conv="clr-namespace:AacV2.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Yu Gothic UI" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Yu Gothic UI" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="behaviors:DwellBehavior.IsEnabled" Value="True" />
            <Setter Property="behaviors:DwellBehavior.DwellTimeMs" Value="800" />
            <Setter Property="behaviors:DwellBehavior.CooldownMs" Value="300" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6" />
                            </Border>
                            <Border Background="#220099FF" Height="4" VerticalAlignment="Bottom" CornerRadius="0,0,8,8">
                                <Border.Width>
                                    <MultiBinding Converter="{StaticResource ProgressToWidthConverter}">
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="(behaviors:DwellBehavior.Progress)" />
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth" />
                                    </MultiBinding>
                                </Border.Width>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Yu Gothic UI" />
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <conv:ProgressToWidthConverter x:Key="ProgressToWidthConverter" />
    </Application.Resources>
</Application>
